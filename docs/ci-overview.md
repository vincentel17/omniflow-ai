# CI Overview

Single pipeline (`ci.yml`) validates repository structure, static checks, tests, migrations, security scans, and build integrity.

Branch protection should require all jobs to pass before merge.
